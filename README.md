# 🔐 GlassAuth - PHP Starter Kit

A lightweight, secure authentication boilerplate built with **Vanilla PHP** and **Tailwind CSS**. Designed with a modern "Glassmorphism" UI style.

## 🚀 Features
- **Secure Authentication**: Uses `password_hash` (Bcrypt) and PDO Prepared Statements against SQL Injection.
- **Modern UI**: Custom Glassmorphism design using Tailwind CSS utility classes.
- **Animated Backgrounds**: CSS-only animated blobs for a premium feel.
- **Session Management**: Secure login/logout flow.
- **No Frameworks**: Pure PHP, easy to deploy on any shared hosting.

## 🛠️ Installation
1. Clone the repo.
2. Import `database.sql` into your MySQL database.
3. Rename `config/db.example.php` to `config/db.php` and set your credentials.
4. Open `public/index.php` in your browser.

## 👤 Default User
- **Email:** `demo@example.com`
- **Password:** `admin123`
